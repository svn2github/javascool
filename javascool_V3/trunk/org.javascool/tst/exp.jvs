{
  int k = 1;
  a = 3 * 2 + 3 / (x - z);
  call(toto, titi, f(x) / g(y % z));
  while (k < a) {
    if (k == 2) {
      ok(mec);
    } else if (k == 1) {
      ok(aussi, mec);
    }
    k = k + 1;
  }
}
