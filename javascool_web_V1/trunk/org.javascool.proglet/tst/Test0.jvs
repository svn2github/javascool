
void main() {
  println("Entrer une valeur de départ : ");
  double n = readDouble();
  while (n != 1) {
    // Si n / 2 est un entier pair . . 
    if (Math.round(n / 2) == n / 2 ){
      n = n / 2;
    } else {
      n = 3 * n + 1;
    }
    println("n = " + n);
  }
}
